node_modules/
*.log
.DS_Store
.env
.env.local
.npmrc
coverage/
.nyc_output/
*.tgz
.vscode/
.idea/

# package-lock.json is intentionally NOT ignored — committed for reproducible CI
# but skills themselves are libraries; consumers don't see this lockfile.
